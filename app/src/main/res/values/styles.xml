<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/darker_gray</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/colorWhiteText</item>
    </style>

    <style name="BlackButtonTheme" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorBlackPrimary</item>
        <item name="colorControlHighlight">@color/colorBlackLight</item>
    </style>

    <style name="RedButtonTheme" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorRedPrimary</item>
        <item name="colorControlHighlight">@color/colorRedLight</item>
    </style>

    <style name="BlueButtonTheme" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorBluePrimary</item>
        <item name="colorControlHighlight">@color/colorBlueLight</item>
    </style>


</resources>

